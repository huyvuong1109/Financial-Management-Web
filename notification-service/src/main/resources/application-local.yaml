# Cấu hình local - file này KHÔNG được push lên git
# Sử dụng: chạy với profile "local" (spring.profiles.active=local)

spring:
  datasource:
    # Kết nối đến MySQL container riêng (mysql-moi)
    url: jdbc:mysql://localhost:3306/bank_service
    username: root
    password: 123456
  artemis:
    broker-url: tcp://localhost:61616
    user: artemis
    password: artemis
